// Re-export the command palette hook from the component for convenience.
// Any component that only needs open/close state can import from here
// without pulling in the full palette JSX.
export { useCommandPaletteContext as useCommandPalette } from "@/components/CommandPalette";
